---
# site.yml — мастер-плейбук, запускает все шаги по порядку
# Использование: ansible-playbook -i inventory.yml site.yml

- import_playbook: users.yml
- import_playbook: network.yml
- import_playbook: software.yml
- import_playbook: materials.yml
